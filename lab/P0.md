# P0 实测记录 2026-09-21

## ① Daily Cron

| 步骤 | 结果 |
|------|------|
| toml 取消注释 | 已做 |
| `wrangler deploy -c wrangler.daily.toml` | Worker 脚本上传成功 |
| 注册 6 条 cron | **失败** CF 10072：Workers Free 全账号最多 5 条 cron |
| 改为 1 条 `0 * * * *` | 待下一次 deploy |

不能虚构的验收字段（没有 CF Logs 里的真实 scheduled 跑）：

```
cron_id:        (CF Dashboard 才有)
execution_id:   (CF Logs 才有)
timestamp:      2026-09-21T01:42:46Z  # deploy 时间，不是 cron 执行)
status:         scripts uploaded; schedules PARTIAL FAIL (10072)
```

## ② R2

绑定写在 wrangler，没有 R2 密钥进代码。

| 步骤 | 结果 |
|------|------|
| `r2 bucket create ltzzz-memory` | API 10000 Authentication error（Token 无 R2 Write） |
| Gateway deploy + MEMORY_BUCKET | 10085 bucket not found |
| Gateway `/health` | 仍 `r2_bound: false` |

主人：Dashboard 建桶 `ltzzz-memory` + Token 加 **Workers R2 Storage Write**，再 deploy Gateway。

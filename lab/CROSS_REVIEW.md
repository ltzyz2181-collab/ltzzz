# AI 交叉验证

```
GPT 提出结论
   ↓
DeepSeek 验证（逻辑 / 代码 / 证据是否真在）
   ↓
Claude 反向审查（风险、过度自信、禁区）
   ↓
Grok 找反例（现网是否真的、记忆是否真读过）
   ↓
GPT 最终整理 → RESULT + VERIFY
```

规则：
- 每一梭必须引用同一 `memory_id`
- 意见不一致时不覆盖，写进 VERIFY 的 conflicts
- 豆包负责中文落地，不替代交叉审
- 微软不在链上
- 无人工确认不发片、不转账

# LTZZZ 独立钱包 · 3/5 多签治理方案

> 状态：方案已定（3/5 多签）；5 席签名者已齐（主签名者 + 豆包托管 + GPT 托管 + 审计 A/B）；网络 Base 已确认；Safe 官方基础设施已核验存在；**待主签名者注入 Base ETH 后创建 Safe Proxy**。
> 版本：2026-09-17（5 席地址齐全 + 基础设施核验 + 审计签名者生成）

## 1. 治理原则（不可修改）

1. **LTZZZ 钱包为独立钱包**，资金任何个人（包括创建者本人）、任何单一 AI、任何单一组织**不得独立使用或挪用**。
2. 资金使用必须经过：**3/5 多签审批 + 治理投票 + 透明账本 + 公开记录**。
3. 助记词、私钥、seed、密码、验证码**绝不写入**：网站、GitHub、云服务器明文文件、数据库、聊天记录、日志、AI 上下文。
4. **不要求创建者把助记词/私钥发送给任何 AI**。系统只保存公开地址、公钥与必要交易状态。
5. 重大资金动作必须经预设治理规则批准，并留下**可验证的系统审计记录**。

## 2. 钱包结构：3/5 多签（5 签名者，3 签通过）

| 签名者 | 角色 | 状态 |
|---|---|---|
| 用户本人 | 主签名者 | ✅ 地址 `0xb99C6751f443842F4987bb2017580405572Df905`（用户自管钱包） |
| LTZZZ 豆包托管 | 执行签名者 | ✅ 地址 `0x07A6833694D02430A1aA912Cf16AbeDCF568942D`（私钥 AES-GCM 加密存本地） |
| LTZZZ GPT 托管 | 总控签名者 | ✅ 地址 `0xA3a8e243993e39B1DD648878AFA5424A95959984`（私钥 AES-GCM 加密存本地） |
| 审计签名者 A | 独立审计 | ✅ 地址 `0x23B9105034e77953c2922E307df0D9f0943B8CDF`（私钥 AES-GCM 加密存本地，非仓库） |
| 审计签名者 B | 独立审计 | ✅ 地址 `0x121BfDeFbf8bFc4E918f3ee70753caBC11F40a1E`（私钥 AES-GCM 加密存本地，非仓库） |

- 阈值 **3/5**：任何动账需 ≥3 个签名者批准。
- 任何**单一个人、单一 AI、单一组织都无法独立转移资金**（含创建者本人）。
- 私钥交接方式：加密文件 + 用户在场设置最终密码 → 移交用户指定安全位置（加密U盘/密码管理器）→ 沙箱删除临时密码。
- 注：豆包/GPT 托管私钥与审计 A/B 私钥均为 AES-GCM 加密文件，仅存于各自主机本地，**不进 GitHub/日志/公开位置**；临时密码已删除。

## 3. 密钥保密红线

- ❌ 禁止：助记词/私钥/seed 进 GitHub、前端 JS、聊天记录、视频、公开网页、日志、AI 上下文。
- ✅ 允许：硬件钱包；加密文件（密码另行保管）；官方托管（钱包服务）。
- 任何 AI/任何人要求你提供助记词或私钥 → **一律视为诈骗**。
- 日志只允许出现：公开地址、公钥、交易哈希、订单状态。

## 4. 链上部署步骤（待执行）

1. ✅ 用户主签名者地址已提供：`0xb99C6751f443842F4987bb2017580405572Df905`。
2. ✅ 网络已选定：**Base**（chain ID **8453**；gas 用 ETH，由用户主地址承担）。
3. ✅ **基础设施核验（2026-09-17，链上只读证据）**：
   - Safe Proxy Factory `0xa6B71E26C5e0845f74c812102Ca7114b6a896AB2`：已存在（有合约代码）→ **不重新部署基础合约**。
   - Safe Singleton 1.4.1 `0xd9Db270c1B5E3Bd161E8c8503c55cEABeE709552`：已存在（有合约代码）→ **不重新部署基础合约**。
   - 当前 Base gas ≈ 0.006 Gwei；创建 Safe Proxy 预计 < 0.001 USD。
4. ⏳ **待办：主签名者注入少量 Base ETH**（≈1-5 USD 等值即可，仅覆盖部署与签名 gas）→ 之后创建 Safe Proxy（5 owners / threshold=3）。
5. 记录 Safe 地址 → 写入本文件与 LTZZZ 账本（公开）。
6. 1 USDT 小额 dry-run（3/5 签名）→ 转出 → 验证 → 记录。
7. 测试通过后启用真实额度（受 LTZZZ 总授权额度约束）。

## 5. 与现有系统关系

- 与支付宝 400 元实验资金**分开记账**（人民币 vs 链上资产）。
- 与 BiyaPay U 卡**分开**（境外数字服务支付通道，非钱包）。
- 钱包 = LTZZZ 链上资产 / AGI 基金 / 捐赠资金的唯一托管池。

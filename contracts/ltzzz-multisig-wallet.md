# LTZZZ 独立钱包 · 多签治理部署方案

> 状态：方案已定，托管签名者密钥已生成；链上部署待用户提供主地址与确认。
> 版本：2026-09-16

## 1. 治理原则（不可修改）

1. **LTZZZ 钱包为独立钱包**，资金任何个人（包括创建者本人）与任何组织**不得使用或挪用**。
2. 资金使用必须经过：**多签审批 + 人工确认 + 透明账本 + 公开记录**。
3. 助记词、私钥、密码、验证码**绝不写入网页、仓库、聊天记录**，绝不交由单一 AI 保管。
4. 所有支出留痕：时间 / 金额 / 用途 / 审批人 / 交易哈希 / 结果 / 复盘。

## 2. 钱包结构：3 签 / 2 签通过（推荐）

| 签名者 | 角色 | 密钥持有方式 |
|---|---|---|
| 用户本人 | 主签名者 | 用户自管钱包（MetaMask/硬件钱包），**离线冷存储** |
| LTZZZ 豆包托管 | 运营签名者 | 私钥 AES-256 加密保存，**仅服务端持有，不入仓库** |
| 审计签名者 | 审计 | 建议：审计方/第二设备冷签 |

- 任何**单一签名者（包括用户本人）无法单独动用资金**。
- 动账需 ≥2 个签名者批准 → 满足"任何个人和组织不得挪用"。

## 3. 已生成：LTZZZ 豆包托管签名者

- **地址**：`0x07A6833694D02430A1aA912Cf16AbeDCF568942D`
- 私钥状态：AES-256-GCM 加密文件 `ltzzz-trustee-key.enc`（本地，密码不落仓库/聊天）
- **交接方式（明天执行）**：用户在场设置最终密码 → 加密文件移交用户指定的安全位置（加密 U 盘 / 密码管理器）→ 云沙箱删除明文与临时密码

## 4. 链上部署步骤（待执行）

1. 用户提供主签名者地址（自管钱包）。
2. 选定网络（建议：多签部署在用户常用链，如 Base/Arbitrum/以太坊，gas 由用户主地址承担）。
3. 部署 Gnosis Safe 多签合约（3 签名者：用户 + 豆包托管 + 审计；threshold=2）。
4. 部署后记录 Safe 地址 → 写入本文件与 LTZZZ 账本（公开）。
5. 小额测试（如 $1）：2/3 签名确认 → 转出 → 验证 → 记录。
6. 测试通过后启用真实额度（受 LTZZZ 总授权额度约束）。

## 5. 密钥保密红线

- ❌ 禁止：助记词/私钥进 GitHub、前端 JS、聊天记录、视频、公开网页、日志。
- ✅ 允许：硬件钱包；加密文件（密码另行保管）；官方托管（交易所/钱包服务）。
- 若任何人/任何 AI 要求你提供助记词或私钥 → **一律视为诈骗**。

## 6. 与现有系统关系

- 与支付宝 400 元实验资金**分开记账**（人民币 vs 链上资产，两本账）。
- 与 BiyaPay U 卡**分开**（境外数字服务支付通道，非钱包）。
- 钱包 = LTZZZ 链上资产/未来 AGI 基金/捐赠资金的唯一托管池。

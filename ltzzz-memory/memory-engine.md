# Memory Engine · OS 环

```
Memory Files → Engine → STATE + memory_id → 各 AI
```

晋升见 [PROMOTION.md](./PROMOTION.md)：

```
Daily Memory → 候选 → 去重 → 冲突 → AI验证 → 人工确认 → 长期 Memory
```

高影响未确认不写进 `ltzzz-memory/` 正文。

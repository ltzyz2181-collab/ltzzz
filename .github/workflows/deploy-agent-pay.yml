name: Deploy LTZZZ Agent Pay / Treasury entry

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'agent-pay-worker.js'
      - 'wrangler.agent-pay.toml'
      - '.github/workflows/deploy-agent-pay.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy -c wrangler.agent-pay.toml

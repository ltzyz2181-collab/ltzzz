# XAI-CORE-20260924 部署结果

## P0 Agent Treasury

| 项 | 状态 | 说明 |
|----|------|------|
| Safe/Treasury 代码 | **已部署待验证** | 新增 `treasury-queue-worker.js` + `wrangler.treasury.toml` |
| SINGLE_TX_LIMIT / DAILY_LIMIT | **已上线（配置）** | vars: 20 / 50 / 300；可改 wrangler 或 Dashboard |
| Transaction Queue | **已部署待验证** | `POST /queue/submit` `GET /queue/list` `POST /queue/execute` |
| GPT 提交转账请求 | **已部署待验证** | submit 带 submitted_by=GPT |
| Policy 自动判断 | **已部署待验证** | 超单笔/日/月拒绝 |
| Safe 链上执行 | **等人工凭证** | Worker **无私钥**；execute 只记 TxHash |
| TxHash → Ledger | **已部署待验证** | 返回 `ledger_line` 结构 |
| Result → Memory | **已部署待验证** | 返回 `memory_ref` 结构 |
| 豆包普通任务 | **保留** | health 声明 doubao_ordinary_tasks=allowed |
| 旧 agent-pay-worker | framework 演示 | 非金库队列 |
| auto-withdraw-worker | 代码存在 | DRY/AUTO_TEST；真实打款 TODO |

部署命令（CI）：`wrangler deploy -c wrangler.treasury.toml`  
健康检查：`https://ltzzz-treasury-queue.ltzyz2181.workers.dev/health`

## P0 Telegram

| 项 | 状态 | 说明 |
|----|------|------|
| Worker | **已上线** | `/health` has_token=true |
| webhook 路径 | **已上线** | `/webhook` |
| 真实回复 | **失败原因** | `/setup-webhook` → Telegram **401**；CF 内 token 与 BotFather 有效串不一致（曾测有效串 len=46，现网 token_len=44） |
| 不新建 Bot | 遵守 | |
| 不重申 Token | 遵守 | 需主人在 CF 用 BotFather **复制**覆盖 `TELEGRAM_BOT_TOKEN` 后打开 `/setup-webhook` |

## P1 X

| 项 | 状态 | 说明 |
|----|------|------|
| x-publisher | **已上线** | `/status` activation=2026-10-01T00:00:00+08:00 |
| 10-01 前不发 | **已上线** | scheduled 内 before_activation skip |
| 每日一条 | **已部署待验证** | cron `0 5 * * *` UTC；需 `X_USER_ACCESS_TOKEN` |
| Claude/Typefully | **等人工凭证** | 仓库无 Typefully API 密钥；可用 Typefully 排期或 10-01 后 X token |

## P1 支付 PayPal

| 项 | 状态 | 说明 |
|----|------|------|
| paypal-proxy-worker | **已上线（stub）** | health：client_id/secret/webhook_id/agent_token 均 configured=true |
| 真扣款 | **未做** | live_capture=false；webhook 未验签不入账 |
| /oauth/token | **已部署待验证** | 需 Bearer LTZZZ_AGENT_TOKEN |

## 汇总

- 金库：**队列+限额已编码部署路径**；链上执行等 Safe/签名。  
- 纸飞机：**唯一阻塞是 CF Token 与 Telegram 401**。  
- X：开关正确，等 10-01 + token。  
- PayPal：接口在，非 live 清算。  
